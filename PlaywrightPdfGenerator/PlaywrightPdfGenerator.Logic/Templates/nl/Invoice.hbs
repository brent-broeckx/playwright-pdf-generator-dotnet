<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Factuur</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Source Code Pro", monospace;
            color: #414141;
            background-color: #ffffff;
            padding: 2rem;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #007ab3;
        }

        .logo {
            width: 140px;
        }

        .invoice-title {
            font-size: 2rem;
            font-weight: 700;
            color: #007ab3;
            text-align: right;
        }

        .invoice-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 2rem;
        }

        .billing-info, .invoice-details {
            flex: 1;
        }

        .billing-info {
            margin-right: 2rem;
        }

        .section-title {
            font-size: 1rem;
            font-weight: 700;
            color: #007ab3;
            margin-bottom: 0.75rem;
            text-transform: uppercase;
        }

        .info-line {
            font-size: 0.875rem;
            line-height: 1.6;
            margin-bottom: 0.25rem;
        }

        .info-label {
            font-weight: 600;
            display: inline-block;
            width: 120px;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin: 2rem 0;
        }

        .items-table thead {
            background-color: #007ab3;
            color: #ffffff;
        }

        .items-table th {
            padding: 0.75rem;
            text-align: left;
            font-weight: 700;
            font-size: 0.875rem;
        }

        .items-table td {
            padding: 0.75rem;
            border-bottom: 1px solid #d9d9d9;
            font-size: 0.875rem;
        }

        .items-table tbody tr:nth-child(even) {
            background-color: #f5f5f5;
        }

        .text-right {
            text-align: right;
        }

        .text-center {
            text-align: center;
        }

        .totals-section {
            display: flex;
            justify-content: flex-end;
            margin-top: 1rem;
        }

        .totals {
            width: 300px;
        }

        .total-line {
            display: flex;
            justify-content: space-between;
            padding: 0.5rem 0;
            font-size: 0.875rem;
        }

        .total-line.subtotal {
            border-top: 1px solid #d9d9d9;
        }

        .total-line.tax {
            color: #666;
        }

        .total-line.grand-total {
            border-top: 2px solid #007ab3;
            font-weight: 700;
            font-size: 1.125rem;
            color: #007ab3;
            margin-top: 0.5rem;
            padding-top: 0.75rem;
        }

        .footer {
            margin-top: 3rem;
            padding-top: 1.5rem;
            border-top: 1px solid #d9d9d9;
            font-size: 0.75rem;
            line-height: 1.6;
            color: #666;
            text-align: center;
        }

        .notes {
            margin-top: 2rem;
            padding: 1rem;
            background-color: #f5f5f5;
            border-left: 4px solid #007ab3;
        }

        .notes-title {
            font-weight: 700;
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
        }

        .notes-content {
            font-size: 0.875rem;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <img src="" alt="loremcomp" class="logo">
        <h1 class="invoice-title">FACTUUR</h1>
    </div>

    <!-- Invoice Info -->
    <div class="invoice-info">
        <div class="billing-info">
            <h3 class="section-title">Factureren aan:</h3>
            <div class="info-line"><strong>{{CustomerName}}</strong></div>
            <div class="info-line">{{CustomerAddress}}</div>
            <div class="info-line">{{CustomerCity}}, {{CustomerZip}}</div>
            <div class="info-line">{{CustomerCountry}}</div>
            <div class="info-line" style="margin-top: 0.5rem;">E-mail: {{CustomerEmail}}</div>
        </div>
        
        <div class="invoice-details">
            <h3 class="section-title">Factuurgegevens:</h3>
            <div class="info-line">
                <span class="info-label">Factuur #:</span>
                <span>{{InvoiceNumber}}</span>
            </div>
            <div class="info-line">
                <span class="info-label">Datum:</span>
                <span>{{InvoiceDate}}</span>
            </div>
            <div class="info-line">
                <span class="info-label">Vervaldatum:</span>
                <span>{{DueDate}}</span>
            </div>
            <div class="info-line">
                <span class="info-label">Polis #:</span>
                <span>{{PolicyNumber}}</span>
            </div>
        </div>
    </div>

    <!-- Items Table -->
    <table class="items-table">
        <thead>
            <tr>
                <th>Omschrijving</th>
                <th class="text-center">Aantal</th>
                <th class="text-right">Eenheidsprijs</th>
                <th class="text-right">Bedrag</th>
            </tr>
        </thead>
        <tbody>
            {{#each Items}}
            <tr>
                <td>{{Description}}</td>
                <td class="text-center">{{Quantity}}</td>
                <td class="text-right">€{{UnitPrice}}</td>
                <td class="text-right">€{{Amount}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <!-- Totals -->
    <div class="totals-section">
        <div class="totals">
            <div class="total-line subtotal">
                <span>Subtotaal:</span>
                <span>€{{Subtotal}}</span>
            </div>
            <div class="total-line tax">
                <span>BTW ({{TaxRate}}%):</span>
                <span>€{{TaxAmount}}</span>
            </div>
            <div class="total-line grand-total">
                <span>TOTAAL:</span>
                <span>€{{Total}}</span>
            </div>
        </div>
    </div>

    <!-- Notes -->
    {{#if Notes}}
    <div class="notes">
        <div class="notes-title">Opmerkingen:</div>
        <div class="notes-content">{{Notes}}</div>
    </div>
    {{/if}}

    <!-- Footer -->
    <div class="footer">
        <p>Bedankt voor uw vertrouwen!</p>
        <p>loremcomp Verzekeringen | BTW: BE0123456789 | IBAN: BE71 0961 2345 6769</p>
        <p>Voor vragen over deze factuur kunt u contact met ons opnemen via billing@loremcomp.be</p>
    </div>
</body>
</html>
